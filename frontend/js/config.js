// Configuration settings for the application
const CONFIG = {
    // API endpoint (change this to your backend API URL)
    API_URL: 'http://localhost:8000',
    
    // Default profile information
    DEFAULT_PROFILE: {
        name: 'Atharva Takle',
        title: 'IT Professional',
    },
    
    // LinkedIn post settings
    POST_SETTINGS: {
        maxLength: 50000, // Maximum post length in characters
        defaultHashtags: ['networking', 'professional', 'career'] // Default hashtags
    }
};